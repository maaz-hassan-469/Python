# using os module to find the directory path of the folder
import os
print(os.listdir('/'))
# we can also write as given below
directory_path="/"
content=os.listdir(directory_path)
print("directory_path")